{
  "name": "nifty-ema-alerts",
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Python serverless functions - no build required'",
    "vercel-build": "echo 'Python serverless functions - no build required'",
    "start": "echo 'Starting serverless functions'"
  },
  "dependencies": {},
  "engines": {
    "node": "18.x"
  }
}
